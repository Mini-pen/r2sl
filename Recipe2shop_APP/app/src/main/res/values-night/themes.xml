<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Recipe2shoplist" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Couleurs principales de la marque -->
        <item name="colorPrimary">@color/primary_blue_light</item>
        <item name="colorPrimaryVariant">@color/primary_blue</item>
        <item name="colorOnPrimary">@color/black</item>
        
        <!-- Couleurs secondaires -->
        <item name="colorSecondary">@color/secondary_green_light</item>
        <item name="colorSecondaryVariant">@color/secondary_green</item>
        <item name="colorOnSecondary">@color/black</item>
        
        <!-- Couleurs d'accent -->
        <item name="colorAccent">@color/accent_teal_light</item>
        
        <!-- Couleurs de fond -->
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="colorSurface">@color/neutral_gray_dark</item>
        <item name="colorOnSurface">@color/white</item>
        
        <!-- Couleurs de texte -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/neutral_gray_light</item>
        
        <!-- Couleur de la barre de statut -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        
        <!-- Personnalisation du thème -->
        <item name="android:windowBackground">@color/background_dark</item>
    </style>
</resources>